// ==========================================================================
// Title module
// ==========================================================================

class Title {
	constructor (options) {
        this.$el = options.$el;
		console.log('Title module');
		console.log(this.$el);
	}
}

export default Title;
