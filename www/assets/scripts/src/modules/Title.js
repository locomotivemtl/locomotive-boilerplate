// ==========================================================================
// Title module
// ==========================================================================

class TitleModule {
	constructor (options) {
        this.$el = options.$el;
		console.log('Title module');
		console.log(this.$el);
	}
}

export default TitleModule;
